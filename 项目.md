------

你现在要执行一个**长时自主工程任务（Long-Horizon Autonomous Project）**。

你的角色不是问答助手，而是一个**自主推进的 AI 工程师 + 技术文档作者 + 教学设计者**。

你的任务目标是：

> 从零构建一个完整的、面向初学者的 Transformer 教学项目，让一个完全没有 Transformer 实现经验的人，仅依靠项目内文档，就能够从零完成环境搭建、代码运行、原理理解和实验验证。

项目名称：

**From-Scratch Transformer Lab（从零实现 Transformer 教学实验室）**



项目根目录，放在当前项目中，创建work_02目录，并按照下方指引搭建项目结构

------

# 一、最终交付目标（必须全部完成）

最终必须交付一个**完整工程项目**，包括：

### 1. 可运行代码

至少包含以下模块：

- embedding（词嵌入）
- positional encoding（位置编码）
- scaled dot-product attention（缩放点积注意力）
- multi-head attention（多头注意力）
- encoder block（编码器块）
- decoder block（解码器块）
- full transformer（完整 Transformer）
- train.py（训练脚本）
- inference.py（推理脚本）

要求：

- 可直接运行
- 无缺失文件
- 无伪代码

------

### 2. 完整目录结构

必须生成：

```text
transformer_lab/
│── data/
│── src/
│   ├── embedding.py
│   ├── positional_encoding.py
│   ├── attention.py
│   ├── multi_head_attention.py
│   ├── encoder.py
│   ├── decoder.py
│   ├── transformer.py
│   ├── train.py
│   └── inference.py
│
│── configs/
│── tests/
│── docs/
│── notebooks/
│── experiments/
│── README.md
│── guide.md
│── requirements.txt
```

目录必须合理。

------

# 二、guide.md（最重要）

必须面向“完全新人”。

假设读者：

- 会一点 Python
- 没写过 Transformer
- 没搭过深度学习项目

guide.md 必须覆盖：

------

## 1. 环境搭建

例如：

```bash
python -m venv .venv
pip install -r requirements.txt
```

不仅写命令，还要解释：

- 为什么这样做
- 做完后会得到什么

------

## 2. 数据准备

使用：

Tiny Shakespeare

说明：

- 为什么选它
- 从哪里下载
- 放到哪个目录
- 数据格式是什么

------

## 3. 运行步骤

逐步说明：

```bash
python train.py
```

会发生什么：

- 输出什么
- loss 应该怎样变化
- 如何判断正常

------

## 4. 调试方法

例如：

```python
print(x.shape)
```

解释：

为什么先检查 shape。

------

## 5. 常见错误

必须覆盖：

- ModuleNotFoundError
- CUDA out of memory
- tensor shape mismatch
- loss 不下降

并给出解决方案。

------

# 三、docs 文档体系

docs 目录必须包含：

------

### 01_transformer_overview.md

解释：

- Transformer 为什么出现
- RNN 为什么不够

------

### 02_attention.md

解释：

- Q / K / V 是什么
- 为什么这么设计

公式：

softmax(QK^T / √dk)

要解释公式含义，不只是写公式。

------

### 03_multi_head.md

解释：

为什么要多头。

多个 head 分别学什么。

------

### 04_residual_and_norm.md

解释：

- 为什么需要残差连接
- 为什么需要 LayerNorm

------

### 05_training.md

解释：

- loss 如何下降
- 什么是过拟合
- 如何观察训练过程

------

文档目标：

> 让读者“理解为什么”。

不是只看代码。

------

# 四、代码规范（必须严格）

所有代码必须：

------

## 1. 每个文件有头部说明

例如：

```python
"""
attention.py
实现缩放点积注意力机制
"""
```

------

## 2. 每个函数有注释

例如：

```python
def forward(x):
    """
    输入:
        x: [batch, seq_len, d_model]

    输出:
        tensor
    """
```

------

## 3. 每个关键 tensor 标 shape

例如：

```python
# x: [batch, seq_len, d_model]
```

必须全覆盖。

因为目标读者是新人。

------

# 五、测试（必须）

生成 tests/

至少包含：

- shape test
- forward pass test
- attention correctness
- training smoke test
- inference test

要求：

```bash
pytest
```

必须全部通过。

------

# 六、实验（必须）

生成 experiments/

包含：

------

### baseline

标准训练。

------

### 去掉 positional encoding

观察影响。

------

### 修改 head 数

例如：

1 → 2 → 4 → 8

观察变化。

------

### 修改 d_model

观察效果。

------

每个实验必须写：

- 改了什么
- 为什么改
- 结果如何
- 学到了什么

不是只贴 loss。

------

# 七、硬件限制

必须满足：

- 支持硬件切换--[cpu←→gpu]

默认配置建议：

```python
d_model = 128
n_heads = 4
num_layers = 2
seq_len = 64
```

不要设计过大模型。

------

# 八、自检机制（每完成一个模块都执行）

完成任何模块后，必须执行：

```text
Self-check:
1. 代码能运行吗？
2. 测试通过了吗？
3. guide.md 更新了吗？
4. docs 更新了吗？
5. 注释完整了吗？
6. 新人能看懂吗？
```

如果有一项不满足，继续修复。

不得跳过。

------

# 九、工作方式

你必须：

- 自主拆分任务
- 自主排序优先级
- 自主推进
- 不等待人工确认
- 不在中途停止

除非：

- 全部交付完成
- 所有测试通过
- 文档完整
- guide 完整
- 可以交给新人使用

才允许结束。

最终目标：

> 产出一个“新人拿到就能从零搭建 Transformer”的完整教学工程。